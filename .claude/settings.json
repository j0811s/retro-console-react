{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.ts|*.tsx) npx eslint --fix \"$f\" || true;; esac; }",
            "statusMessage": "Running eslint --fix"
          }
        ]
      }
    ]
  }
}
